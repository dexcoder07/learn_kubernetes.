apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-replicasets
spec:
  replicas: 4
  selector:
    matchLabels:
      tier: frontend
  
  template:
    metadata:
      labels:
        tier: frontend
    
    spec:
      containers:
        - name: nginx-container
          image: nginx:latest